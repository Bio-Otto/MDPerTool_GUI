# Quick Start

Once you have installed MDPerTool, you can start using it either via the Command Line Interface (CLI) or the Graphical User Interface (GUI).

## Starting the GUI
To launch the main interface:
```bash
mdpertool gui
```

## Running via CLI
To see the available command line arguments:
```bash
mdpertool cli --help
```